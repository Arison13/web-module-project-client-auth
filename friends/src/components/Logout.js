import React, {useEffect} from "react";
import { useHistory } from 'react-router-dom';

const Logout = () => {

    return(
        <div>
            <h2>Logout</h2>
        </div>
    )
}

export default Logout